import * as actionTypes from '../actions/ActionTypes'

const employees = (state, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default employees

/*
 Selectors
*/
